// take integer as input and print half of the number.
#include <iostream>
using namespace std;
int main()
{
    int num;
    cout << "enter a integer: ";
    cin >> num;
    float x = float(num);
    cout << "half of the following number is: " << (x / 2) << endl;
    return 0;
}