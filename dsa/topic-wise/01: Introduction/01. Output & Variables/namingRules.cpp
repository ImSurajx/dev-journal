#include <iostream>
using namespace std;
int main()
{
    // start with _ or $ is not  allowed
    // some keywords are not allowed
    // starting with numbers are not allowed
    int name, x, y;
    int 1x;
    return 0;
}