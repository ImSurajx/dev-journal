#include <iostream>
using namespace std;
int main()
{
    int x, y = 3, 4; // most of the time gives error, not a standard way of variable declaration
    cout << x << " " << y << endl;
    return 0;
}