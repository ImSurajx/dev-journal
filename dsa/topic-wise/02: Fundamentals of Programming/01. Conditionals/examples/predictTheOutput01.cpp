#include <iostream>
using namespace std;
void main()
{
    int number = 0;
    if (number > 0)
        cout << "Number is positive.";
    else
        (number = > 0) cout << "Number is not negative."; // error
}