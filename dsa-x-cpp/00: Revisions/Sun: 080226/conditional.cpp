#include <iostream>
using namespace std;
int main()
{
    /*
    // ===============================
    // CONDITIONALS – REVISION NOTES
    // ===============================

    // CONDITIONALS:
    // Used to make decisions based on conditions.
    // Code executes different blocks depending on true / false.

    // --------------------------------
    // if statement
    // --------------------------------
    // Executes code when condition is true.

    if (condition) {
        // runs if condition is true
    }

    // --------------------------------
    // if - else
    // --------------------------------
    // Executes one block if true, another if false.

    if (condition) {
        // true block
    } else {
        // false block
    }

    // --------------------------------
    // if - else if - else
    // --------------------------------
    // Used when multiple conditions are checked.
    // Order matters: first true condition executes.

    if (condition1) {
        // block 1
    } else if (condition2) {
        // block 2
    } else {
        // default block
    }

    // --------------------------------
    // NESTED if
    // --------------------------------
    // One if inside another if.
    // Used when second decision depends on first.

    if (condition1) {
        if (condition2) {
            // runs when both are true
        }
    }

    // --------------------------------
    // RELATIONAL OPERATORS
    // --------------------------------
    // >   greater than
    // <   less than
    // >=  greater than or equal to
    // <=  less than or equal to
    // ==  equal to
    // !=  not equal to

    // --------------------------------
    // LOGICAL OPERATORS
    // --------------------------------
    // &&  AND  → all conditions must be true
    // ||  OR   → any one condition true
    // !   NOT  → reverses condition

    // Example:
    if (a >= b && a >= c) {
        // a is greatest or equal
    }

    // --------------------------------
    // IMPORTANT EDGE CASE RULE
    // --------------------------------
    // Always think about equality cases.
    // Use >= or <= when ties should be allowed.

    // --------------------------------
    // switch statement
    // --------------------------------
    // Used when a variable has fixed choices.
    // Cleaner than multiple if-else.

    switch (choice) {
        case 1:
            // block
            break;
        case 2:
            // block
            break;
        default:
            // runs if no case matches
    }

    // --------------------------------
    // ternary operator
    // --------------------------------
    // Short form of if-else.
    // Used only for simple conditions.

    result = (condition) ? value1 : value2;

    // --------------------------------
    // COMMON MISTAKES
    // --------------------------------
    // 1. Using = instead of ==
    // 2. Wrong condition order
    // 3. Missing equality handling
    // 4. Forgetting break in switch
    // 5. Overusing nested if

    // --------------------------------
    // THINKING BEFORE CODING
    // --------------------------------
    // 1. What is being checked?
    // 2. What are possible outcomes?
    // 3. Any boundary or equal values?
    // 4. Which condition should run first?

    // --------------------------------
    // REVISION CHECK
    // --------------------------------
    // If you can:
    // - explain logic in words
    // - predict output before running
    // - handle equality cases
    // then conditionals are clear.

    // ===============================
    // CONDITIONALS – DONE
    // ===============================
    */
    return 0;
}
