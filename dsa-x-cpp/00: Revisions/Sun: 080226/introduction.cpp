#include <iostream>
using namespace std;

int main()
{

    // OUTPUT:
    // cout is used to display output on the screen.

    // INPUT:
    // cin is used to take input from user and store it in a variable.

    // VARIABLES:
    // Variable = named memory location used to store data.
    // Data type decides size and type of data stored.

    // DATA TYPES:
    // Primitive: int, float, double, char, bool
    // Non-primitive: array, string, vector

    // OPERATORS:
    // Arithmetic: + - * / %
    // Relational: > < >= <= == !=
    // Increment / Decrement: ++ --

    // TYPE CASTING:
    // Converting one data type into another.
    // Example: char c = char(65);

    // ASCII:
    // A-Z : 65–90
    // a-z : 97–122
    // 0-9 : 48–57

    // OPERATOR PRECEDENCE:
    // (), *, /, +, -
    // Use brackets to avoid confusion.

    return 0;
}